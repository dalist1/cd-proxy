{
  "name": "cd-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal Codex-only ChatGPT OAuth proxy with round-robin credential rotation.",
  "scripts": {
    "start": "zig build -p zig-out >/dev/null 2>&1 || true; bun run src/server.ts",
    "dev": "CD_PROXY_DEBUG=1 bun --watch src/server.ts",
    "check": "zig build -p zig-out >/dev/null 2>&1 || true; bun run src/server.ts --check",
    "zig:build": "zig build -p zig-out",
    "zig:check": "zig build run --",
    "test": "zig build test && bun run scripts/granular-round-robin-test.ts && bun run scripts/test-websocket-flow.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
